# Namespace: Tạo namespace demo cho API workload
apiVersion: v1
kind: Namespace
metadata:
  name: demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    # Lab 2.2: Policy Controller chỉ enforce namespace có label này
    policy.sigstore.dev/include: "true"