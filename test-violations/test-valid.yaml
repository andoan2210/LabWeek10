# Test: Pod hợp lệ -> phải PASS
apiVersion: v1
kind: Pod
metadata:
  name: test-valid
  namespace: demo
spec:
  containers:
  - name: nginx
    image: nginx:1.25.0
    securityContext:
      runAsUser: 1000
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
